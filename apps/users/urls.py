"""
URLs para o aplicativo users.
"""
from django.urls import path
from . import views

app_name = 'users'

urlpatterns = [
    # Adicionar URLs do aplicativo users aqui
    # path('profile/', views.profile, name='profile'),
]
