"""
URLs para o aplicativo utils.
"""
from django.urls import path
from . import views

app_name = 'utils'

urlpatterns = [
    # Adicionar URLs do aplicativo utils aqui
    # path('health/', views.health_check, name='health_check'),
]
